language: bash
sudo: required
services:
- docker
script:
- "./docker-build.sh --extract-dist"
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist/
  on:
    branch: master
